# Tavrix Menu Shared Types

Placeholder TypeScript package for future shared API and domain types.

Build:

```bash
pnpm --filter @tavrix-menu/shared-types build
```

Keep this package focused on stable contracts that are useful across the API and web apps.
